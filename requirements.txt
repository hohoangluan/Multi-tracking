# MTMC pipeline dependencies — uncomment as each module gets implemented.
# The stub scaffold itself needs NONE of these (pure stdlib).

# --- core ---
# numpy
# opencv-python          # M0 decode, M1 undistort/homography, crops

# --- detection / tracking (M2, M3) ---
ultralytics            # YOLOv8/YOLO11 detector + built-in ByteTrack/BoT-SORT tracker
torch
torchvision
lap                    # Hungarian solver ultralytics prefers over scipy when available
# scipy                  # ultralytics falls back to this for Hungarian if lap is absent
# filterpy               # Kalman filter (optional) — not used; ultralytics has its own

# --- ReID (M5) ---
# torchreid              # OSNet and other ReID backbones

# --- metrics (M11) ---
# motmetrics             # IDF1 / MOTA
# trackeval              # HOTA
